-- 코드를 입력하세요
SELECT O.HOUR,
       COUNT(I.ANIMAL_ID) COUNT
FROM ANIMAL_OUTS I RIGHT OUTER JOIN
(
    SELECT LEVEL-1 HOUR FROM DUAL CONNECT BY LEVEL >= 0 AND LEVEL <= 24
) O ON TO_CHAR(I.DATETIME, 'HH24') = O.HOUR
GROUP BY O.HOUR
ORDER BY O.HOUR;